# Q4 Findings: Entitlements, Info.plist keys, and App Capabilities

## Summary of What Exists

The project has **no dedicated `.entitlements` file and no committed `Info.plist` file**. The Info.plist is auto-generated by Xcode, and all capability/Info.plist configuration lives inline in `SingleThread.xcodeproj/project.pbxproj`.

### 1. Entitlements files
- **None found.** `find` for `**/*.entitlements` and a full-repo grep for `CODE_SIGN_ENTITLEMENTS` / `.entitlements` returned zero matches.
- No `SystemCapabilities` section exists in `project.pbxproj` (grep returned no matches).

### 2. Info.plist
- **No physical `Info.plist` file exists** (`find **/Info.plist` → none).
- `GENERATE_INFOPLIST_FILE = YES` is set for all three targets (app, unit tests, UI tests):
  - App Debug: `project.pbxproj:400`
  - App Release: `project.pbxproj:444`
  - Tests Debug/Release: `project.pbxproj:483`, `:509`
  - UITests Debug/Release: `project.pbxproj:534`, `:559`

### 3. App capabilities / build settings (app target)
App target Debug config (`51AA3EFA`) and Release config (`51AA3EFB`) are identical for these:

| Setting | Debug | Release | Meaning |
|---|---|---|---|
| `ENABLE_APP_SANDBOX = YES` | `:396` | `:440` | App Sandbox enabled (macOS) |
| `ENABLE_HARDENED_RUNTIME = YES` | `:397` | `:441` | Hardened Runtime enabled (macOS) |
| `ENABLE_USER_SELECTED_FILES = readonly` | `:399` | `:443` | User-selected file access, read-only (macOS sandbox) |
| `REGISTER_APP_GROUPS = YES` | `:418` | `:462` | App Groups registration enabled |
| `CODE_SIGN_STYLE = Automatic` | `:393` | `:437` | Automatic signing |
| `DEVELOPMENT_TEAM = 6NWX2DHB9Q` | `:395` | `:439` | (also project-level `:307`, `:369`) |
| `PRODUCT_BUNDLE_IDENTIFIER = app.alanvardy.SingleThread` | `:416` | `:460` | |
| `SUPPORTED_PLATFORMS = "iphoneos iphonesimulator macosx xros xrsimulator"` | `:421` | `:465` | iOS/macOS/visionOS multi-platform |
| `TARGETED_DEVICE_FAMILY = "1,2,7"` | `:427` | `:471` | iPhone, iPad, visionOS(7) |
| `IPHONEOS_DEPLOYMENT_TARGET = 26.5` | `:411` | `:455` | |
| `MACOSX_DEPLOYMENT_TARGET = 26.5` | `:414` | `:458` | |
| `XROS_DEPLOYMENT_TARGET = 26.5` | `:428` | `:472` | visionOS |

**Note on App Groups:** `REGISTER_APP_GROUPS = YES` only enables the capability registration toggle; there is **no `com.apple.security.application-groups` entitlement** (no `.entitlements` file, no group identifier string), so no actual App Group is declared or usable.

### 4. INFOPLIST_KEY_* generated Info.plist keys (app target)
All present in both Debug (`:401`–`:410`) and Release (`:445`–`:454`):

- `INFOPLIST_KEY_UIApplicationSceneManifest_Generation[sdk=iphoneos*]` = YES (`:401` / `:445`)
- `INFOPLIST_KEY_UIApplicationSceneManifest_Generation[sdk=iphonesimulator*]` = YES (`:402` / `:446`)
- `INFOPLIST_KEY_UIApplicationSupportsIndirectInputEvents[sdk=iphoneos*]` = YES (`:403` / `:447`)
- `INFOPLIST_KEY_UIApplicationSupportsIndirectInputEvents[sdk=iphonesimulator*]` = YES (`:404` / `:448`)
- `INFOPLIST_KEY_UILaunchScreen_Generation[sdk=iphoneos*]` = YES (`:405` / `:449`)
- `INFOPLIST_KEY_UILaunchScreen_Generation[sdk=iphonesimulator*]` = YES (`:406` / `:450`)
- `INFOPLIST_KEY_UIStatusBarStyle[sdk=iphoneos*]` = `UIStatusBarStyleDefault` (`:407` / `:451`)
- `INFOPLIST_KEY_UIStatusBarStyle[sdk=iphonesimulator*]` = `UIStatusBarStyleDefault` (`:408` / `:452`)
- `INFOPLIST_KEY_UISupportedInterfaceOrientations_iPad` = portrait/upside-down/landscape left/right (`:409` / `:453`)
- `INFOPLIST_KEY_UISupportedInterfaceOrientations_iPhone` = portrait/landscape left/right (`:410` / `:454`)

### 5. Privacy / permission keys and system data/service access
- **No privacy usage-description keys** (`NSCameraUsageDescription`, `NSLocationWhenInUseUsageDescription`, `NSPhotoLibraryUsageDescription`, etc.). Full-repo grep for `NSUsage` / `UsageDescription` returned zero matches.
- **No system service access in source code.** Grep of `SingleThread/` for `Camera`, `Microphone`, `Location`, `Photos`, `Contacts`, `Calendar`, `Reminder`, `EventKit`, `HealthKit`, `HomeKit`, `Bluetooth`, `CoreLocation`, `UserNotifications`, `AVFoundation`, `PhotoKit`, `ContactsUI`, `CoreBluetooth`, `requestAccess`, etc. returned **no matches**.
- Swift sources import only `SwiftData`, `SwiftUI`, and `Foundation`:
  - `SingleThread/SingleThreadApp.swift:7-8`
  - `SingleThread/ContentView.swift:7-8`
  - `SingleThread/Item.swift:6-7`

### 6. Conclusion
The app requests **no access to any system data or services** (no privacy keys, no permission-triggering frameworks). The only configured "capabilities" are the macOS App Sandbox, Hardened Runtime, and read-only user-selected-files access (all standard for the macOS slice of this multi-platform target), plus the App Groups registration toggle with no actual entitlement/group declared.