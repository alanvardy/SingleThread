# Done

- **What was built**: macOS-only refresh button in `SingleThread/ContentView.swift` now swaps its `arrow.clockwise` label for a `ProgressView()` spinner (inside the control plate) while `viewModel.isRefreshing` is true, plus `.accessibilityValue("Refreshing")` — mirroring the `BackgroundSettingsView` precedent. Lockstep structural assertion `contentViewBodyContainsRefreshButtonOnMacOS` updated for the new `_ConditionalContent` signature. State machine (`refreshManual`, ≥1 s hold via `MinimumDisplayDuration`) was pre-existing and untouched.
- **Commit SHA(s)**: `5b85b614` — "feat: show refresh spinner in macOS refresh button" (pushed to `origin/alanvardy-var-797-show-refresh-spinner-on-macos`; only `SingleThread/ContentView.swift` + `SingleThreadTests/SingleThreadTests.swift` changed).
- **Verification**: `make format` clean; `swiftlint lint --strict` exit 0; `make mac-build` BUILD SUCCEEDED; targeted `make mac-test` (`-only-testing:SingleThreadTests`, macOS destination) — 595 passed including `contentViewBodyContainsRefreshButtonOnMacOS`; 3 failures are the documented pre-existing local-only `EntitlementStoreTests` (CI green). Full `./scripts/test.sh` gate deferred to `run-gate` per convention.
- **Reviewer findings**: no blockers. Nits (optional): the assertion is static-type-only (matches repo precedent — no runtime branch toggle test); `.font(.title3)` on `ProgressView` is cosmetic, verify visually.
- **Remaining manual items**: on-device visual check that the spinner shows for ≥1 s while refreshing (manual UX check from the ticket); full CI gate once via `run-gate` later; `DELETEME` removal at merge time.