# Done

- **What was built**: Fixed the iPad (A16) CI leg regression in the two `AppViewModelSyncWiringTests` reschedule tests. Root cause: `AppViewModel.setupSyncService` guarded on `WCSession.isSupported()`, which is `false` on iPad (WatchConnectivity is iPhone-only), leaving `syncService` nil so both `#require(appViewModel.syncService)` calls unwound — not a timing race. Fix: threaded an optional `session: (any SkipSyncSession)? = nil` through `AppViewModel.init` → `setupSyncService`; an injected session (tests pass `FakeSession()`) bypasses the hardware gate, while the nil production path stays byte-for-byte identical (gate + `WCSession.default`). Both tests now run and pass on both matrix legs.
- **Commit SHA(s)**: `72060532` — "fix: wire sync service in wiring tests on iPad via injected session" (pushed fast-forward to `origin/alanvardy-var-1017-main-is-red-appviewmodelsyncwiringtests-reschedule-tests`, `0a7fd13e..72060532`)
- **Verification**: Red-first proven on iPad (A16) sim (`SIM='platform=iOS Simulator,id=37F525B1-…'`) — both cases failed pre-fix, exact CI match. Post-fix: `./scripts/test-one.sh 'SingleThreadTests/AppViewModelSyncWiringTests' 900` → `ok: 2 case(s) ran` on **both** iPad (A16) and iPhone 17 (pinned `.simulator_id`) sims. `swiftformat` clean on both files; `swiftlint lint --strict` 0 violations (188 files). Full `./scripts/test.sh` gate still pending (run-gate skill).
- **Reviewer findings**: 0 blockers, 0 nits. Reviewer verified the nil/session-identity collapse (`session ?? WCSession.default`), lazy `WCSession.default` access, production callers unchanged (`SingleThreadApp.swift:52`, migration/mic tests), type conformance of `FakeSession`/`WCSession` to `any SkipSyncSession`, and the receive path still exercising the real AppViewModel→service→store wiring.
- **Remaining manual items**: (1) `git rm DELETEME` before merge (bootstrap marker, still unstaged). (2) Full CI-identical gate `./scripts/test.sh` via the run-gate skill (worker ran targeted suites only, per SMALL bounds). (3) `DELETEME` and `.pi/` artifacts left unstaged intentionally.