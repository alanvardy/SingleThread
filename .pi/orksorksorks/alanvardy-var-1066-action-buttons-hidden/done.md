# Done

- **What was built**: Fixed the iOS main-screen bug where the action-button cluster (Complete · mic · Skip) stayed hidden on app open until a partial swipe forced a re-render. Added an `@Observable` stored `var hasLoadedReminders` to `ContentViewModel` that flips to `true` right after `await store.start()` returns (reminders settled), and folded it into the `showsActionButtons` gate — so the cluster recomputes and appears as soon as a visible reminder lands, without user interaction. `ContentView.swift` needed no edit (the render branch already reads the observable-backed gate).
- **Commit SHA(s)**: `95a274d1611878f70404f764272e0e4cea08b1da` ("Fix action buttons hidden on open until reminders settle"), pushed to `origin/alanvardy-var-1066-action-buttons-hidden`.
- **Verification**: `scripts/test-one.sh SingleThreadTests/ActionButtonTests` → `ok: 6 case(s) ran` (5 pre-existing happy/sad cases + 1 new regression), exit 0. `swiftformat SingleThread/ SingleThreadTests/` → 0 files reformatted. `swiftlint lint --strict SingleThread/ SingleThreadTests/` → 0 violations, 0 serious. Sim destination pinned from `.simulator_id`.
- **Reviewer findings**: No blockers from either review pass. Two optional P2 notes, both accepted: (1) the regression test is red at build-time rather than a pure behavioral red because it references the new flag (a cleaner behavioral red isn't practical); (2) `hasLoadedReminders` is set unconditionally after `store.start()` — if `start()` ever threw the cluster stays hidden, but that path is unreachable in practice (gate stays correctly false via `visibleReminders` otherwise). A raised doc-comment spacing nit (line 77) was a false positive — verified single-space in the committed file.
- **Remaining manual items**: Full CI-identical gate (`make check` / `./scripts/test.sh`) not run per this SMALL task's targeted-verify scope — recommended before merge to confirm the iOS/macOS build + broader single-thread suite under `SWIFT_TREAT_WARNINGS_AS_ERRORS=YES`. Also, per repo AGENTS.md, the `DELETEME` marker is still tracked-deleted in the working tree and `.pi/orksorksorks/…/` artifacts are untracked — both must be handled (not folded) before the branch merges.