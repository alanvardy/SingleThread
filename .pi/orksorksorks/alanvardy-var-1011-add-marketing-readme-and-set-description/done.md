# Done

- **What was built**: Consumer-facing marketing `README.md` at the repo root — hero line, selling points adapted from the alanvardy.dev/SingleThread marketing page, and prominent official Apple "Download on the App Store" badges (black for light theme, white for dark, via GitHub's `<picture>`/`prefers-color-scheme`) linking to the App Store page; the two official badge SVGs added under `assets/`; the `DELETEME` bootstrap marker removed; GitHub repo description confirmed unchanged.
- **Commit SHA(s)**: `52808c37` (chore: remove DELETEME bootstrap marker), `f13e95c5` (docs: add marketing README with App Store download badges), `2ae36a7c` (docs: add trailing newline to README)
- **Verification**: No markdown/SVG lint exists in this repo (CI lint is Swift-only; gitleaks secret-scan is a no-op for these files). Verified: official badge variants proven exact (`_blk_`/`_wht_` in SVG titles, toolbox en-us endpoints, unmodified); all 4 `assets/` refs in README resolve; App Store URL matches the live marketing page; repo description via `gh repo view alanvardy/SingleThread --json description` reads "SingleThread shows one Apple Reminder at a time for calm, focused momentum." (kept, per ticket intent). Swift build/gate not applicable — no code changed.
- **Reviewer findings**: No blockers. Nits: "Fast. Smooth. **Calm.**" reads like a typo but is verbatim brand voice from the live marketing page (confirmed against `templates/singlethread.html`), kept; `width="180"` without height renders correctly from the SVGs' intrinsic 119.66:40 aspect; missing trailing newline — fixed in `2ae36a7c`.
- **Remaining manual items**: None for the branch. Merge the PR with `--rebase` per repo convention.