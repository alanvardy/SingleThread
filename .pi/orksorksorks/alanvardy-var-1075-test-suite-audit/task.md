# Task

Audit the SingleThread codebase's test suite with parallel subagents: refactor
code to be as unit-testable as possible, find opportunities to write additional
valuable unit tests, and add them. The audit spans all modules (SingleThread
app, SingleThreadCore, watch app, widget, and the existing test targets).
Actively raise issues with the user when found.