# Task — VAR-685: Change projects to lists

The app currently refers to groups of reminders as "projects", but Apple Reminders calls them "lists". Rename the concept throughout the codebase — user-facing strings (e.g. "Excluded Projects" settings screen), internal identifiers, persistence keys, WatchConnectivity payloads, and tests — to use "list" terminology for consistency with the platform.

Linear issue: VAR-685 (https://linear.app/vardy/issue/VAR-685/change-projects-to-lists)
