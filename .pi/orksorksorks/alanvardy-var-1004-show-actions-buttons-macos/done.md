# Done

- **What was built**: Fixed VAR-1004 — the macOS Settings "Show action buttons" toggle now shows/hides the bottom-bar action buttons. `ContentView.bottomBar`'s macOS branch gates `actionButtons` on `viewModel.showsActionButtons` (mirroring iOS) instead of only `store.visibleReminders.first != nil`; the `enableActionButtons` mirror + predicate in `ContentViewModel` and the view→view-model feeds (`.task`, `.onChange`) were opened from `#if os(iOS)` to `#if os(iOS) || os(macOS)`. iOS behavior, the App Group key, and persistence are unchanged. `ContentView+ActionMenu.swift` needed no change (`macShowActionMenu` reads the same AppStorage-backed preference; cluster referenced only from the gated `bottomBar`).
- **Commit SHA(s)**: `015f985f` (VAR-1004 fix: 3 files, +10/−9) · `b237d648` (Periphery fix 1: `isShowingPurchase` direct references, +35/−33) · `8a80735e` (Periphery fix 2: drop dead `WatchFakeSession.lastMessage`, +1/−3) · plus `071f4738`/`6713c1b7` chore markers (completion marker, gate-verdict record). All pushed to `origin/alanvardy-var-1004-show-actions-buttons-macos`.
- **Verification**:
  - In-line: `make format` ✓ · `make lint` 0 violations / 0 serious (188 files) ✓ · `make test` (macOS-native, 604 cases) ✓ except the three known local-only CI-green StoreKit tests (`EntitlementStoreTests.isEntitledSurvivesStoreRecreation`, `initialRefreshSettlesResolvedFlag`, `hostStoreKitIsClean` — per AGENTS.md). Reopened guard confirmed: all 5 `ActionButtonTests` + 3 `MacOSActionButtonChromeTests` executed and passed on macOS.
  - **Full `./scripts/test.sh` gate (run-gate skill, tip `8a80735e`): GREEN-EQUAL.** Format ✓ · SwiftLint ✓ · iOS build ✓ · watch build ✓ · **Periphery: "No unused code detected."** ✓ · iOS UI smoke (`testLaunchAndRenderSmoke`) ✓ · watch UI smoke ✓ (one pre-launch `CoreSimulator Code=405` flake self-recovered) · watch units ✓ · macOS units 657 passed / 4 failed = 3 annotated pre-existing StoreKit tests + 1 proven-environmental flake (`PreferenceHolderTests/refreshesOnNotification` — re-run in isolation passes in 0.106s; branch diff touches no PreferenceHolder code). No regression anywhere.
  - **The two earlier local-gate Periphery blockers were fixed in-scope**, not waived: (1) `isShowingPurchase` — the local Xcode 27 front-end drops lone `$`projection references from the indexed graph (CI's Xcode 26.6 counts them; `showAbout` in `SingleThreadApp.swift` is bit-identical in shape and unflagged); fixed by reading/writing the state directly from the inlined `upgradePrompt` button (moved to the Bottom Bar extension to stay under `type_body_length`), deleting the now-unused `UpgradePromptButton`. (2) `WatchFakeSession.lastMessage` — genuinely assign-only watch-test-fixture code surfaced because the local scan indexes watch-test targets (CI does not); removed the dead property + assignment.
  - UI test intentionally not added — the gate logic lives in the testable `showsActionButtons` view-model predicate.
- **Reviewer findings**: No blockers — APPROVE (pass 3). Two P2 nits, both inherent to the mandated "mirror iOS" pattern and deferred: (1) first-render flash on macOS when the toggle is persisted OFF (mirror defaults `true` until `.task` injects — strict improvement over the pre-fix permanent cluster); (2) `macShowActionMenu` reads the `@AppStorage` directly while the outer gate reads the view-model mirror — they converge via the feeds (identical pre-existing iOS asymmetry).
- **Remaining manual items**:
  - Mark PR #199 ready for review (currently draft). CI on the pushed branch is authoritative; the local macOS-unit trio + the local watch-stage config caveats below do not affect CI (fresh runners, Xcode 26.6).
  - Environment learnings worth capturing in the run-gate skill / AGENTS.md (proposed, not yet applied): (a) local Periphery scans on Xcode 27 can false-positive on `$`projection-only `@State` — prefer direct references; (b) the watch-stage destination must be pinned as `WATCH_TEST_SIM='platform=watchOS Simulator,id=3F69EA19-301C-4978-AA7B-A63DE7CE69F5'` with the watch **unpaired** — the name-only `Apple Watch Series 11 (46mm)` default is unresolvable when both watchOS 26.5 and 27.0 runtimes are installed, and pairing the watch to the worktree phone launches the XCTest runner on a phone clone and fails.
  - Optional manual macOS check: toggle "Show action buttons" in Settings with a visible reminder and confirm the cluster hides/shows; note toggle-off also removes the `c`/`s` keyboard shortcuts since they live on the hidden buttons (same as iOS).