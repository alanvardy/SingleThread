# Done

- **What was built**: The `.hitRegion` flake was already resolved in this branch's base: main's "Collapse the superseded UI surface" (e46f94b0) deleted `ActionButtonsUITests.swift` (incl. `testActionButtonsAccessibilityAudit`), and the sole iOS UI test `testLaunchAndRenderSmoke` already restricts `performAccessibilityAudit` to `[.sufficientElementDescription, .trait]` — dropping `.hitRegion`/`.dynamicType` locally, matching CI (acceptance arm b). This commit removes the stale reference to the deleted suite in `SingleThreadTests/ActionButtonTests.swift:12-17` (now points at the smoke + its carve-out) and drops the workflow DELETEME marker.
- **Commit SHA(s)**: `60c7538a` (docs: point ActionButtonTests at the collapsed UI smoke for a11y coverage)
- **Verification**: macOS-native unit suite green — 589 cases passed, only the three known pre-existing local `EntitlementStoreTests` failures (`isEntitledSurvivesStoreRecreation`, `initialRefreshSettlesResolvedFlag`, `hostStoreKitIsClean`; CI-green, don't debug). iOS UI suite on the pinned worktree simulator (`platform=iOS Simulator,id=C8ACE16D-…`): `TEST SUCCEEDED`, `SingleThreadUITests.testLaunchAndRenderSmoke` passed (78.7 s). `swiftformat --lint` and `swiftlint lint --strict` clean on the changed file. Full `./scripts/test.sh` gate not run (SMALL step; targeted verification only).
- **Reviewer findings**: Subagent backends were unavailable this session (dead model ids / no provider API keys for launched children), so the review pass ran inline. No blockers: change satisfies both acceptance arms and no test is a known-expected local failure (proven by the green local sim run). Nits: the new doc comment is 6 lines (acceptable — it records the resolution); N/A otherwise.
- **Remaining manual items**: The iOS-compiled variant of `ActionButtonTests.swift` (`#if os(iOS)` block) compiles only under CI's iOS unit leg — comment-only change, no compile risk. Simulator `C8ACE16D` left booted. If desired, merge the PR (`gh pr merge <n> --rebase --delete-branch`).