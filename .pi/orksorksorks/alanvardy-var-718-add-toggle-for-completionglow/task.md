# Task

The SingleThread app shows a brief full-screen green flash ("CompletionGlow")
after a reminder is completed, on both the iPhone/macOS app and the Apple
Watch. Today this feedback is always on and cannot be disabled. This task adds
a settings toggle that lets the user turn the CompletionGlow off, so the glow
is suppressed after a successful completion when the preference is disabled.