# Done

- **What was built**: The privacy policy screen (Settings → Privacy Policy) now renders in the in-app interface language. `PrivacyGuideContent.localized(_:in:)` pins resolution via `LocalizedStringResource(...).resolved(in: locale)` (the codebase's established pattern) instead of `String(localized:…locale:)` (which silently fell back to the development language for non-English). `PrivacySettingsView` reads `@Environment(\.locale)` and threads it through `sections(in:)`/`closingLine(in:)`, so switching language re-renders the open screen via the root `\.locale` injection (LocalizedNavigationTitle mechanism). Added `import SingleThreadCore` to the two app-side files that now use the package's `resolved(in:)` extension.
- **Commit SHA(s)**: `fbe7b803` (fix: resolve privacy policy copy in the interface language) · `1069a2b8` (chore: remove DELETEME bootstrap marker). Pushed to `alanvardy-var-1046-translate-privacy-policy` (c2d11f90..1069a2b8).
- **Verification**: On the worktree's iOS simulator with the exact CI unit-test config (`-parallel-testing-enabled NO -maximum-concurrent-test-simulator-destinations 1`, build-for-testing + test-without-building), the four relevant suites ran **32 tests, 32 passed, 0 failed**: PrivacySettingsContentTests (4, incl. the new `privacyGuideContentResolvesInEveryInterfaceLanguage` and `privacyGuideContentNonEnglishValuesDifferFromEnglish`), SettingsViewTests (16), LocalizationTests (5), AppLanguageTests (7). `swiftformat` + `swiftlint --strict` clean on all touched files.
- **Reviewer findings**: Independent review returned **no blockers** (merge OK). Two **P2 nits deferred**: (1) `SettingsViewTests/privacySettingsViewContainsExpectedContent` stays host-locale-dependent — same as `origin/main` (not a regression); the suggested `.environment(\.locale, …)` pin is what empirically **crashes** the body-stringification on this toolchain (`ModifiedContent.body` → SwiftUI `View.bodyError()` SIGTRAP), which is why that file was reverted to stock. (2) Live language-switch re-render is verified by mechanism (env dependency + SwiftUI re-evaluation) rather than a UI test — a UI test isn't justified per the repo's testing policy.
- **Remaining manual items**: The **macOS unit-test lane does not compile locally** ("cannot find type 'SkipSyncSession' in scope" in `SingleThread/AppViewModel.swift:22`) — a pre-existing issue in files untouched by this diff (byte-identical to `origin/main`); CI's `mac-tests` job (fresh runners, Xcode 26.6) is authoritative. The full `./scripts/test.sh` gate was not run here (SMALL process = targeted verify only); the changed app files compiled cleanly in the macOS compile batch (only the pre-existing `AppViewModel` error failed that lane).
