# Task — VAR-690 "Show list"

When a reminder is displayed on the card, also show the name of the list (calendar) it belongs to. Add a "Show list" toggle to settings to control this. Additionally, rename two existing settings labels: "Show undated" → "Show undated reminders" and "Enable action buttons" → "Show action buttons".

Linear: https://linear.app/vardy/issue/VAR-690/show-list
Branch: `alanvardy-var-690-show-list` (draft PR #94 already attached to the issue)
