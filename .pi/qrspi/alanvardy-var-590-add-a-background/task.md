# Task — Add a background (VAR-590)

Fetch a nature photograph from `GET vardy.cc/unsplash`, store it locally, and
display it behind the main reminder list with a 50% fade. Refresh the stored
image if it is more than an hour old; if a fetch fails, keep showing the last
successfully stored image (or no background if none exists). Add a settings
toggle to disable the background, and credit the photographer in the settings
menu.

Linear: VAR-590 · Branch: `alanvardy-var-590-add-a-background` · Draft PR #92.
