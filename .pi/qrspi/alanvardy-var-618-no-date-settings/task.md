# Task

Add a "Show tasks with no date" toggle to the settings screen. When enabled,
reminders that have no due date — which the app currently does not display at
all — should appear alongside dated reminders. The toggle should persist and
behave consistently with the existing settings preferences.