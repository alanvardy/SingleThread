# Q4 — Test coverage and injection seams for the preference layer

Repo-relative paths throughout. Findings verified by static analysis only (read/grep/find); no builds or tests run.

## 1. Per-preference unit-test files

The six preference types live in `SingleThreadCore/Sources/SingleThreadCore/`: `ShowAlarmsPreference.swift`, `ShowCompletionGlowPreference.swift`, `ShowDatePreference.swift`, `ShowListPreference.swift`, `ShowRecurrencePreference.swift`, `ShowUndatedRemindersPreference.swift`. All six share the constructor shape `public init(defaults: UserDefaults = AppGroup.defaults, key: String = "<defaultsKey>")` — at `ShowAlarmsPreference.swift:11`, `ShowCompletionGlowPreference.swift:11`, `ShowDatePreference.swift:11`, `ShowListPreference.swift:11`, `ShowRecurrencePreference.swift:11`, `ShowUndatedRemindersPreference.swift:9-11`. Default keys and absent-value fallbacks:
- `"showAlarms"` → `as? Bool ?? true` (`ShowAlarmsPreference.swift:11,20`)
- `"showCompletionGlow"` → `?? true` (`ShowCompletionGlowPreference.swift:11,20`)
- `"showDate"` → `?? true` (`ShowDatePreference.swift:11,20`)
- `"showList"` → `?? false` (`ShowListPreference.swift:11,20`)
- `"showRecurrence"` → `?? true` (`ShowRecurrencePreference.swift:11,20`)
- `"showUndatedReminders"` → `load()` = `as? Bool ?? false`, `save(_:)` at `:22` (`ShowUndatedRemindersPreference.swift:18-22`; this one exposes `load()/save()` instead of `isEnabled/set`)

Five direct per-preference test files exist in `SingleThreadTests/`, all using the Swift Testing framework (`import Testing`):

| File | Preference covered | Absent-value assertion | Round-trip |
|---|---|---|---|
| `ShowAlarmsPreferenceTests.swift` | `ShowAlarmsPreference` | `#expect(preference.isEnabled, "missing key defaults to enabled")` `:11` | `set(false)` `:12-13`, `set(true)` `:14-15` |
| `ShowCompletionGlowPreferenceTests.swift` | `ShowCompletionGlowPreference` | `:11` plus guard `preference.isEnabled != false` `:12-14` | `:15-18` |
| `ShowDatePreferenceTests.swift` | `ShowDatePreference` | `:11` plus guard `preference.isEnabled != false` `:13-15` | `:16-19` |
| `ShowListPreferenceTests.swift` | `ShowListPreference` | `#expect(!preference.isEnabled, "absent key defaults to disabled (new feature preserves today's card look)")` `:11-13` (inverse default) | `set(true)` `:14-15`, `set(false)` `:16-17` |
| `ShowRecurrencePreferenceTests.swift` | `ShowRecurrencePreference` | `:11` | `:12-15` |

Custom UUID-key-on-`.standard` pattern — identical shape in every file (`ShowAlarmsPreferenceTests.swift:8-10`; `ShowCompletionGlowPreferenceTests.swift:8-10`; `ShowListPreferenceTests.swift:8-10`):
```
let key = "<prefix>-test-\(UUID().uuidString)"   // showalarms-test-… / showcompletionglow-test-… / showdate-test-… / showlist-test-… / showrecurrence-test-…
defer { UserDefaults.standard.removeObject(forKey: key) }
let preference = Show<X>Preference(defaults: .standard, key: key)
```
All five construct the store on `UserDefaults.standard` (never `AppGroup.defaults`) with a per-test UUID key, assert the absent-value fallback first, then save/read round-trip both polarities.

Render-level (not store) preference tests also exist: `ShowAlarmsTests.swift` (`ReminderCardView` bell with `showAlarms:`/`hasAlarms:`, `:9-27`), `ShowDateTests.swift` (date + list rows via `showDate:`/`showList:`/`listName:`, `:10-38`), `ShowRecurrenceTests.swift` (recurrence row `:9-27`). There is no `ShowCompletionGlowTests.swift`; iOS glow state machine + `ContentViewModel` wiring live in `CompletionGlowTests.swift:13-46` (state machine) and `:48-105` (`CompletionGlowViewModelTests`, incl. `glowStaysInactiveWhenPreferenceDisabled` `:83-93`, `glowTriggersWhenPreferenceEnabled` `:95-105`).

## 2. SkippedReminderSyncServiceTests and WatchSyncPipelineTests

### Shared abstraction: the `SkipSyncSession` protocol
`SkippedReminderSyncService.swift:8-15` defines `public protocol SkipSyncSession: AnyObject { func activate(); func updateApplicationContext(_:) throws; func sendMessage(_:replyHandler:errorHandler:) }`; `extension WCSession: SkipSyncSession {}` at `:17`. Doc comment at `:7`: "Test seam: WCSession is not mockable, so we abstract the calls we need."

### iOS fake — `FakeSession`
`SkippedReminderSyncServiceTests.swift:9-28`: `final class FakeSession: SkipSyncSession` with stored `activated`, `lastContext`, `lastMessage`, `pushShouldThrow`; `updateApplicationContext` throws `NSError(domain: "test", code: 1)` when `pushShouldThrow`. File wrapped in `#if os(iOS) || os(watchOS)` (`:1`). Suite struct `SkippedReminderSyncServiceTests` at `:35` (`@MainActor`).

### Watch fake — `WatchFakeSession`
`WatchSyncPipelineTests.swift:9-25`: `private final class WatchFakeSession: SkipSyncSession`. Header comment `:8`: "Private copy of the iOS suite's fake — it cannot be imported across test bundles." Suite struct `WatchSyncPipelineTests` at `:34` — "Watch-target compilation of the sync pipeline plus the Phase 1–3 behaviors, asserted natively on watchOS where the iOS-side bundle cannot run" (`:32-33`).

### Test-side helpers bridging the divergent concrete store types
Every test injects concrete stores via the divergent-but-identical `defaults: .standard, key: <uuid>` initializer, e.g. `SkippedReminderSyncServiceTests.swift:43,80-87` (`SkippedReminderStore`, `ExcludedListStore`, `ShowUndatedRemindersPreference`, `SortOptionStore`, `ShowDatePreference` under one UUID `suffix`) and `WatchSyncPipelineTests.swift:41-48`. Explicit bridging helpers:
- `makeTestSortStore()` — `SkippedReminderSyncServiceTests.swift:570-574`; comment "An isolated sort store that never touches `AppGroup.defaults`"; returns `SortOptionStore(defaults: .standard, key: "test-sort-store-\(UUID().uuidString)")`.
- Relaunch-seam trio in `WatchSyncPipelineTests.swift` (MARK `// MARK: Private — relaunch-test seam`): `private struct PreferenceValue { let currentValue: Bool }` (`:411`); `makePreference(forContextKey:defaults:storageKey:)` returning `PreferenceValue(currentValue: defaults.object(forKey: storageKey) as? Bool ?? false)` (`:417-424`); `makeService(forContextKey:session:storageKey:)` (`:427-455`) — a `switch contextKey` over `"showUndatedReminders"`, `"showRecurrence"`, `"showAlarms"`, `"showList"`, `"showCompletionGlow"` wiring the matching concrete `Show*Preference` under one `storageKey`. Doc comment `:405-410`: "The five concrete preference types diverge only in default and key, so the relaunch shape unifies behind this wrapper." Consumed by `receivedPreferenceSurvivesRelaunch` (`:164`; `@Test(arguments:)` list `:157-162`) — applies a context value, discards the service, reads a fresh store back.
- Reminder fixture `inListReminder(title:list:)` in both bundles: `SkippedReminderSyncServiceTests.swift:641-649` and file-level `WatchSyncPipelineTests.swift:468-476` — `EKReminder` in a calendar titled `list`; "Construction only — never saved through EventKit."

Other patterns: receive paths call `service.session(WCSession.default, didReceiveApplicationContext:)` / `didReceiveMessage:` directly (`SkippedReminderSyncServiceTests.swift:115-125,291-299`; `WatchSyncPipelineTests.swift:216-235`; service handler at `SkippedReminderSyncService.swift:114-117`). Two-struct split for SwiftLint body limits: `SkipCountSyncTests` (`:578`) and `EntitlementSyncTests` (`SingleThreadTests/EntitlementSyncTests.swift:11-12`, "Reuses the target-level `FakeSession`").
## 3. Serialized-suite tests — `ShowCompletionGlowStateTests` and peers

`SingleThreadWatchTests/ShowCompletionGlowStateTests.swift`:
- `@Suite(.serialized)` at `:27`; rationale comment `:22-25`: "Serialized because every test writes the same real UserDefaults key (\"showCompletionGlow\") via the holder's hardcoded `.standard` store; running them in parallel lets one test's `set(false)`/cleanup race another's read of the default (`true`)."
- What is serialized: the real `UserDefaults.standard` key `"showCompletionGlow"`, seeded `UserDefaults.standard.set(false, forKey: "showCompletionGlow")` (`:34`) and cleaned `defer { UserDefaults.standard.removeObject(forKey: "showCompletionGlow") }` (`:35,44-45,66`).
- What is re-inflated: `ShowCompletionGlowState()` reads the persisted preference in `init()` (`ShowCompletionGlowState.swift:10-13`: `init() { isEnabled = preference.isEnabled }`), with `private let preference = ShowCompletionGlowPreference(defaults: .standard)` (`ShowCompletionGlowState.swift:27`); `apply(_:)` persists + republishes (`:18-23`). Tests assert `ShowCompletionGlowPreference(defaults: .standard).isEnabled` after `apply` (`ShowCompletionGlowStateTests.swift:38-39`).
- Launch-arg pre-set test `uiTestingGlowFlagsPreSetState` (`:51`) parameterized over `("--ui-testing-glow-disabled", false)` (`:48`) and `("--ui-testing-glow", true)` (`:49`): builds `WatchAppViewModel(arguments: [arg.flag])`, asserts `appViewModel.showCompletionGlowState.isEnabled == arg.expectedEnabled`.
- Fixture: `@MainActor private let sharedWatchEventStore = EKEventStore()` (`:11`) + `watchReminder()` (`:15-19`); backing store must outlive reminders (weak ref, SIGTRAP note `:9-10`).

Other `@Suite(.serialized)` suites: `ReminderStoreTests.swift:15`, `:511` (ReminderStoreSkipCountTests), `:720` (UndoCompletionTests), `:898` (ReloadPendingCompletionTests); `CompletionGlowTests.swift:13` ("Serialized so these small timing-sensitive tests never interleave… mirrors `ReminderStoreTests`" `:10-11`) and `:48` (CompletionGlowViewModelTests); `ResumptionGateTests.swift:12`; `CompletionCounterStoreTests.swift:5` ("UUID-backed stores so tests never share state with each other or production" `:7`); `EntitlementStoreTests.swift:8`; `ReminderStoreGateTests.swift:11`; `BackgroundImageStoreTests.swift:8`; `EventKitStoringTests.swift:148,355,502`; `UITestingSeedTests.swift:9`; `BackgroundCardTests.swift:43`; `EntitlementSyncTests.swift:11`; `SingleThreadWatchTests/ReminderStoreWatchTests.swift:26` ("Serialized because every test uses an isolated custom-key `.standard` store", `:23-24`).

## 4. init-parameter injection seams

- `ContentViewModel.init` — `SingleThread/ContentViewModel.swift:14-26`: `init(store: ReminderStore, backgroundImage: BackgroundImageStore, speechTranscriber: any SpeechTranscribing, showCompletionGlow: ShowCompletionGlowPreference = ShowCompletionGlowPreference(), urlOpener: (any URLOpening)? = nil)`. Preference kept private, read "at trigger time so a settings toggle takes effect without rebuilding the view model" (`:216-218`). Exercised by `ContentViewModelTests.swift:16-79` (injects `InMemoryEventStore()`-backed `ReminderStore`, `URLOpeningSpy`) and `CompletionGlowViewModelTests` (`CompletionGlowTests.swift:60-75,89-100`, injects custom-key glow prefs).
- `SkippedReminderSyncService.init` — `SkippedReminderSyncService.swift:26-68`: `session: any SkipSyncSession`, required `skipStore`, ten defaulted store params (`countStore`, `excludeStore`, `sortStore`, `showUndatedStore`, `showDateStore`, `showRecurrenceStore`, `showAlarmsStore`, `showListStore`, `showCompletionGlowStore`, `completionCounter`, `entitlementStore`), and `sendsShowDate/sendsShowRecurrence/sendsShowAlarms/sendsShowList/sendsShowCompletionGlow/sendsEntitled` booleans.
- Store types with injectable `defaults`/`key` inits: `SkippedReminderStore` (`ReminderSkip.swift:124`), `SkipCountStore` (`SkipCountStore.swift:26`), `ExcludedListStore` (`ExcludedListStore.swift:7`), `CompletionCounterStore` (`CompletionCounterStore.swift:19-21`), `SortOptionStore` (`SortOption.swift:25`), `PendingCompletionStore` (`PendingCompletionStore.swift:20-23`), `InMemoryEventStore` (`InMemoryEventStore.swift:16-19`), `ReminderStore` single init with `eventStore: any EventKitStoring = EKEventStore()` (`ReminderStore.swift:23`); `EntitlementStore.init(testingWithEntitled:)` (`EntitlementStore.swift:30-34`) and `init(testingWithEntitlementUnresolved:)` (`:39-41`).
- Watch composition: `WatchAppViewModel.init(arguments: [String] = ProcessInfo.processInfo.arguments)` (`SingleThreadWatch/WatchAppViewModel.swift:13`); `WatchReminderViewModel.init(store:showDateState:showRecurrenceState:showAlarmsState:showListState:showCompletionGlowState:entitlementState:)` (`WatchReminderViewModel.swift:18-28`), exercised in `ShowCompletionGlowStateTests.swift:92-103` and `WatchReminderViewRegressionTests.swift`. Watch state holders hardcode `.standard` prefs: `ShowDateState.swift:28`, `ShowRecurrenceState.swift:28`, `ShowAlarmsState.swift:28`, `ShowListState.swift:28`, `ShowCompletionGlowState.swift:27`.
- iOS composition: `AppViewModel.init(arguments: [String] = ProcessInfo.processInfo.arguments)` (`AppViewModel.swift:21`), `makeStore(arguments:)` (`:273`), `seededStore(_:)` (`:330`), `makeContentViewModel(openURLAction:)` (`:219`).
- View-level bindings: `ReminderSettingsView` takes only `Binding<Bool>`s + `SettingsViewModel` (`ReminderSettingsView.swift:15-24`); `SettingsView.init(bindings:backgroundImage:availableLists:…)` (`SettingsView.swift:14-17`); `SettingsBindings` defaults (`SettingsBindings.swift:22-44`): `showDate: Bool = true`, `showList: Bool = false`, `showRecurrence: Bool = true`, `showAlarms: Bool = true`, `showCompletionGlow: Bool = true`, `showUndatedReminders: Bool = false`. Tested via `SettingsViewTests.swift:148-177` (`reminderSettingsViewContainsExpectedRows`, `.constant` bindings) and `:180-200` (`filterSortSettingsViewContainsExpectedRows`, `showUndatedReminders: .constant(false)` at `:154`).
## 5. Preference/store types LACKING a direct unit-test file

- `ShowUndatedRemindersPreference` — the only one of the six show* preferences with no dedicated test file (`SingleThreadTests/ShowUndatedRemindersPreferenceTests.swift` does not exist). Grep for `ShowUndatedRemindersPreference` in `SingleThreadTests/` matches only `SkippedReminderSyncServiceTests.swift:84-85,132-133,154-157` and `SettingsViewTests.swift:154`; coverage is sync-pipeline push/receive/relaunch and view-bindings only.
- `SkippedReminderStore` — no dedicated test file; `ReminderSkipTests.swift` covers only pure logic (`ReminderSkipLogic.resolve/skipping` `:13-55`, `ReminderPriority`, `ReminderNotesFormatter`, `ReminderSort`). The store's `load()/save()` (`ReminderSkip.swift:131,135`) is exercised only through `ReminderStore`/gate/sync tests (`EventKitStoringTests.swift:334,482,556`; `ReminderStoreGateTests.swift:87,108`; `SkippedReminderSyncServiceTests` throughout, e.g. `:43,54,80`).
- `InMemoryEventStore` — no dedicated test file; used as the injection seam inside `ReminderStoreTests.swift`, `EventKitStoringTests.swift`, `ContentViewModelTests.swift`, `ShowCompletionGlowStateTests.swift`.
- Watch `ShowDateState`, `ShowRecurrenceState`, `ShowAlarmsState`, `ShowListState` — no dedicated state-holder tests; they appear only as constructor arguments in `ShowCompletionGlowStateTests.swift` (`:96-101` etc.) and `WatchReminderViewRegressionTests.swift`. `ShowCompletionGlowState` is the only state holder with a dedicated suite.
- Widget target `SingleThreadWidget` has no test bundle (only `NextThingWidget.swift` + `SingleThreadWidgetBundle.swift`); it reads four preferences via default init at `NextThingWidget.swift:64-67` (`ShowDatePreference().isEnabled`, `ShowListPreference().isEnabled`, `ShowRecurrencePreference().isEnabled`, `ShowAlarmsPreference().isEnabled`) and `AppGroup.defaults.bool(forKey: "showUndatedReminders")` (`:71`).
- All other stores DO have dedicated files: `SkipCountStoreTests.swift`, `CompletionCounterStoreTests.swift`, `ExcludedListStoreTests.swift`, `PendingCompletionStoreTests.swift`, `UndoStoreTests.swift`, `EntitlementStoreTests.swift`, `BackgroundImageStoreTests.swift`, and `SortOptionStoreTests` (inside `SortOptionTests.swift:39-61`).

## 6. UI tests toggling show* preferences, and the launch-arg seams

### Toggle flips (iOS settings screen)
- `SingleThreadUITestsFlows.swift` `testReminderTogglesPersistAcrossRelaunch` (`:419-465`): launches `["--ui-testing", "--reset-glow-preference"]` (`:426`); taps `"Settings"` then `"settingsReminderRow"`; asserts `showListToggle` defaults `"0"` and flips to `"1"` (`:433-436`); asserts `showCompletionGlowToggle` defaults `"1"` and flips to `"0"` (`:438-441`); relaunches with `["--ui-testing"]` (`:448`) and asserts persisted `"1"` (`:452-456`) and `"0"` (`:458-463`).
- `testCompletionGlowDoesNotAppearWhenDisabled` (`:466-490`): `launchSeeded(..., extra: ["--ui-testing-glow"])` (`:469`); flips `showCompletionGlowToggle` to `"0"` (`:476-479`); completes; asserts `otherElements["completionGlowOverlay"]` never appears (`:489`).
- `testCompletionGlowFlashesWhenEnabled` (`:492-512`): `launchSeeded(..., extra: ["--ui-testing-glow"])` (`:496`); asserts `app.otherElements["completionGlowOverlay"]` via `waitForExistence` (`:511`), relying on the 2.0 s duration seam.
- No UI test toggles `showDateToggle`, `showAlarmsToggle`, `showRecurrenceToggle`, or the show-undated control; `testSettingsOpensAndShowsControls` only asserts the `"Show date"` row exists (`SingleThreadUITestsFlows.swift:239`). Watch UI tests toggle nothing — behavior comes solely from launch args (`SingleThreadWatchUITestsFlows.swift:203` `--ui-testing-glow`; `:183` `--ui-testing-gated`; `:27` / `:41` / `:60` / `:110` priority / excluded-list / live-excluded / skip-count).

### Launch-argument string inventory (every string found, definition → consumption)

Test-side helpers: `SingleThreadUITestCase.launchApp(arguments:)` (`SingleThreadUITestCase.swift:14-18`), `launchSeeded(_:extra:)` = `launchApp(arguments: ["--seed", json] + extra)` (`:21-23`), `flipToggle` (`:34-47`), `assertTogglePersists` (`:51-63`), `statusLabel` (`:66-78`).

1. `"--seed"` — defined: `SingleThreadUITestCase.swift:22`; call sites `SingleThreadUITestsFlows.swift:24,35,44,57,76,88,118,150,168,184,225,262,303,367,393,469,496,607,635,668,682,705,718,730,746`, `NotificationsUITests.swift:48,104`, `NotificationsSettingsUITests.swift:7,24`, `NotificationSchedulingUITests.swift:41,65,91,114`, `SkipNudgeUITests.swift:27,64,100`. Consumed: `UITestingSeed.fromLaunchArguments` (`SingleThreadCore/Sources/SingleThreadCore/UITestingSeed.swift:26-36`), called from `AppViewModel.makeStore` (`AppViewModel.swift:274`). Backing: `UITestingSeed.resetPersistedState()` removes the same 24 keys from BOTH `AppGroup.defaults` and `UserDefaults.standard` (`UITestingSeed.swift:38-44`; list `:47-72` incl. `"showDate"`, `"showList"`, `"showRecurrence"`, `"showAlarms"`, `"showCompletionGlow"`, `"showUndatedReminders"`, `"sortOption"`, `"skippedReminderIdentifiers"`, `"skipCounts"`, `"excludedListTitles"`, `"completionCount"`, `"isEntitled"`); `seededStore` then writes `completionCount` and `skipCounts` to `AppGroup.defaults` (`AppViewModel.swift:336,340`) and `"enableActionButtons"` to `.standard` (`:344`).
2. `"--ui-testing"` — defined: `ActionButtonsUITests.swift:25,48`; `SingleThreadUITests.swift:32`; `SingleThreadUITestsFlows.swift:328,351,426,448,518,530,546,557`; `SingleThreadUITestsLaunchTests.swift:31`; `SingleThreadWatchUITests.swift:11,32`; `SingleThreadWatchUITestsFlows.swift:238`; `SingleThreadWatchUITestsLaunchTests.swift:17`. Consumed: iOS `AppViewModel.makeStore` branch (`AppViewModel.swift:284`) and load suppression (`:314`); watch `WatchAppViewModel.init` (`WatchAppViewModel.swift:14`) → `uiTestingStore(arguments:)` (`:94-155`). Backing: `.standard` `enableActionButtons` = true (`AppViewModel.swift:291`); in-memory `InMemoryEventStore` on watch (`WatchAppViewModel.swift:136-155`).
3. `"--reset-glow-preference"` — defined: `SingleThreadUITestsFlows.swift:426`. Consumed: `AppViewModel.swift:285-287` — `UserDefaults.standard.removeObject(forKey: "showCompletionGlow")`, inside the `--ui-testing` branch. Clears `UserDefaults.standard` only; production toggle storage is `@AppStorage("showCompletionGlow", store: AppGroup.defaults)` (`ContentView.swift:132`).
4. `"--reset-swipe-preference"` — defined: `SingleThreadUITests.swift:32`; `SingleThreadUITestsFlows.swift:518,530,557`. Consumed: `AppViewModel.swift:288-290` — `UserDefaults.standard.removeObject(forKey: "showSwipePrompt")`.
5. `"--ui-testing-glow"` — defined: `SingleThreadUITestsFlows.swift:469,496`; `SingleThreadWatchUITestsFlows.swift:203`; unit-test arg `ShowCompletionGlowStateTests.swift:49`. Consumed: iOS `AppViewModel.swift:243` (`completionGlow.duration = 2.0`) and `ContentView.swift:315` (`isGlowUITesting`); watch `WatchAppViewModel.swift:50,53` (`showCompletionGlowState.apply(true)`, `isGlowUITesting`) and `WatchReminderView.swift:72` (a11y overlay).
6. `"--ui-testing-glow-disabled"` — no UI-test caller; unit-test arg `ShowCompletionGlowStateTests.swift:48`. Consumed: `WatchAppViewModel.swift:48` (`showCompletionGlowState.apply(false)`).
7. `"--ui-testing-gated"` — defined: `SingleThreadWatchUITestsFlows.swift:183`. Consumed: `WatchAppViewModel.swift:26-28` — `AppGroup.defaults.set(EntitlementStore.freemiumCap, forKey: "completionCount")`.
8. `"--ui-testing-priority <n>"` — defined: `SingleThreadWatchUITestsFlows.swift:27`. Consumed: `WatchAppViewModel.swift:107-110`.
9. `"--ui-testing-skip-count <n>"` — defined: `SingleThreadWatchUITestsFlows.swift:110`. Consumed: `WatchAppViewModel.swift:118-121` — `AppGroup.defaults.set([reminder.calendarItemIdentifier: count], forKey: "skipCounts")`.
10. `"--ui-testing-excluded-list "<list>"` — defined: `SingleThreadWatchUITestsFlows.swift:41`. Consumed: `WatchAppViewModel.swift:131-146`.
11. `"--ui-testing-live-excluded "<list>"` — defined: `SingleThreadWatchUITestsFlows.swift:60`. Consumed: `WatchAppViewModel.swift:131-146` plus `scheduleUITestLiveExcludedDelivery` `:255-262` (delivers a real `didReceiveApplicationContext` 5 s post-launch).
12. `"--ui-testing-notifications"` — defined: `NotificationsUITests.swift:50`; `NotificationSchedulingUITests.swift:43,67,93,116`. Consumed: `AppViewModel.swift:382` (`refreshPendingSummary`) and `ContentView+iOS.swift:16` (`isNotificationsUITesting`).
13. `"--no-reminders"` — defined: `SingleThreadUITestsAppearanceLaunchTests.swift:35,62,96`. Consumed: `AppViewModel.swift:314` (load suppression).
14. `"--url-opener-spy"` — defined: `SingleThreadUITestsFlows.swift:184-186`; `SkipNudgeUITests.swift:100`. Consumed: `ContentView.swift:168` and `AppViewModel.swift:262` (`isURLSpyUITesting`, spy selection `:220-233`); spy type `URLOpeningSpy` (`SingleThread/URLOpening.swift:39-43`), read by `ContentViewModel.lastOpenedURLForUITesting` (`ContentViewModel.swift:70-74`).

### Backing-store summary for the toggle keys
iOS settings toggles read/write the App Group suite: `@AppStorage(..., store: AppGroup.defaults)` for `showUndatedReminders` (`ContentView.swift:115-116`), `sortOption` (`:118-119`), `showDate` (`:121-122`), `showList` (`:124-125`), `showRecurrence` (`:127-128`), `showAlarms` (`:129-130`), `showCompletionGlow` (`:132-133`). `AppGroup.suiteName = "group.app.alanvardy.SingleThread"`, fallback `UserDefaults(suiteName: suiteName) ?? .standard` (`AppGroup.swift:16-23`). Reset seams `--reset-glow-preference` / `--reset-swipe-preference` and the `.standard`-scoped keys `enableActionButtons` / `showSwipePrompt` live in `UserDefaults.standard` (`AppViewModel.swift:291,289`). `AppViewModel.registerDefaults()` registers only `["showMicrophoneButton": true]` on `.standard` (`AppViewModel.swift:207-209`). The watch never presents a settings screen; its five `Show*State` holders bind to `.standard` prefs (`ShowDateState.swift:28`, `ShowRecurrenceState.swift:28`, `ShowAlarmsState.swift:28`, `ShowListState.swift:28`, `ShowCompletionGlowState.swift:27`).
