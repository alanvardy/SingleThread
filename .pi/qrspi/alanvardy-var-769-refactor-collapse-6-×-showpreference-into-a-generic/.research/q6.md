# Q6 — Shared-code constraints on `SingleThreadCore` (iOS / watchOS / widget)

Repo root: `/Users/vardy/dev/alanvardy-var-769-refactor-collapse-6-×-showpreference-into-a-generic`. All paths are repo-relative.

---

## 1. Swift 6 language mode — `SWIFT_VERSION` and `SWIFT_APPROACHABLE_CONCURRENCY`

`SingleThread.xcodeproj/project.pbxproj` contains 16 `XCBuildConfiguration` blocks: 2 project-level (PBXProject "SingleThread") and 14 target-level (7 targets × Debug/Release).

| Config | Debug header (name line) | Release header (name line) |
|---|---|---|
| PBXProject (project-level) | `:617` `51AA3EF7302D5C4500960DFC` | `:680` `51AA3EF8302D5C4500960DFC` |
| SingleThread (iOS/macOS app) | `:735` `51AA3EFA302D5C4500960DFC` | `:785` `51AA3EFB302D5C4500960DFC` |
| SingleThreadTests | `:835` `51AA3EFD302D5C4500960DFC` | `:864` `51AA3EFE302D5C4500960DFC` |
| SingleThreadUITests | `:893` `51AA3F00302D5C4500960DFC` | `:917` `51AA3F01302D5C4500960DFC` |
| SingleThreadWatch (watch app) | `:941` `51AA3F2B000000000000000C` | `:969` `51AA3F2C000000000000000D` |
| SingleThreadWidget | `:997` `51AA3F4B0000000000000000` | `:1028` `51AA3F4C0000000000000000` |
| SingleThreadWatchUITests | `:1059` `51AA3F640000000000000005` | `:1081` `51AA3F650000000000000006` |
| SingleThreadWatchTests | `:1103` `51F0F49900000000000000AA` | `:1127` `51F0F49900000000000000AB` |

Per-target `SWIFT_VERSION = 6.0;` — **all 14 target configs**, none at project level:

- **SingleThread (iOS app)**: `project.pbxproj:780` (Debug), `:830` (Release).
- **SingleThreadTests**: `:858` (Debug), `:887` (Release).
- **SingleThreadUITests**: `:911` (Debug), `:935` (Release).
- **SingleThreadWatch**: `:963` (Debug), `:991` (Release).
- **SingleThreadWidget**: `:1023` (Debug), `:1054` (Release).
- **SingleThreadWatchUITests**: `:1074` (Debug), `:1096` (Release).
- **SingleThreadWatchTests**: `:1120` (Debug), `:1144` (Release).

`SWIFT_APPROACHABLE_CONCURRENCY = YES` — **all 14 target configs** (count = 14; none at project level):

- SingleThread: `:776`, `:826`; SingleThreadTests: `:851`, `:880`; SingleThreadUITests: `:908`, `:932`; SingleThreadWatch: `:960`, `:988`; SingleThreadWidget: `:1020`, `:1051`; SingleThreadWatchUITests: `:1072`, `:1094`; SingleThreadWatchTests: `:1117`, `:1141`.

Other language-mode facts:

- `SWIFT_STRICT_CONCURRENCY` appears **zero** times in the pbxproj (grep count = 0). Strict concurrency checking comes from the Swift 6 language mode; `SWIFT_APPROACHABLE_CONCURRENCY = YES` relaxes the Swift 6 concurrency diagnostics.
- Project-level configs (`:617-734`) set no `SWIFT_VERSION`; they set `SWIFT_TREAT_WARNINGS_AS_ERRORS = YES` at `project.pbxproj:676` (Debug) and `:731` (Release).
- `SingleThreadTests` overrides `SWIFT_TREAT_WARNINGS_AS_ERRORS = NO` (`:856` Debug, `:885` Release) with the comment at `:853-855` / `:882-884`: "StoreKitTest's headers contain an iOS-18-deprecated symbol; with warnings-as-errors the module's PCM fails to emit. SingleThreadTests is the only target importing StoreKitTest, so relax it here."
- `SWIFT_UPCOMING_FEATURE_MEMBER_IMPORT_VISIBILITY = YES` on SingleThread `:779`, `:829`; SingleThreadTests `:857`, `:886`; SingleThreadUITests `:910`, `:934`; SingleThreadWidget `:1022`, `:1053`; SingleThreadWatchTests `:1119`, `:1143`. Not on SingleThreadWatch or SingleThreadWatchUITests.
- `SingleThreadCore/Package.swift:1` — `// swift-tools-version: 6.0`; no `swiftSettings`, no `defaultIsolation`. Platforms `:5-10`: `.iOS("18.7")`, `.watchOS("26.5")`, `.macOS("26.5")`; single library product `:12-13` and `.target(name: "SingleThreadCore")` `:16-18`.
- `.swift-version` (repo root) = `6.0`.
- Documented convention: `AGENTS.md:42-44` — "The compiler language mode is **Swift 6** (`SWIFT_VERSION = 6.0`). `SWIFT_APPROACHABLE_CONCURRENCY = YES` is also set (the Xcode 26 default), which keeps concurrency diagnostics approachable."

---

## 2. Actor-isolation defaults — which targets set `SWIFT_DEFAULT_ACTOR_ISOLATION = MainActor`

Exactly **4 occurrences** in the entire pbxproj (grep count = 4) — the iOS app and the watch app, Debug and Release each:

- **SingleThread (iOS app)**: `SWIFT_DEFAULT_ACTOR_ISOLATION = MainActor;` at `project.pbxproj:777` (Debug config, header `:735`) and `:827` (Release config, header `:785`).
- **SingleThreadWatch (watch app)**: at `project.pbxproj:961` (Debug config, header `:941`) and `:989` (Release config, header `:969`).

**Not set** on:

- **SingleThreadCore package** — `SingleThreadCore/Package.swift` has no isolation setting, so the Swift 6 default (nonisolated) applies; Core opts in explicitly: `ReminderStore` is `@MainActor @Observable public final class ReminderStore` (`SingleThreadCore/Sources/SingleThreadCore/ReminderStore.swift:8`), `EntitlementStore` is `@MainActor @Observable` (`SingleThreadCore/Sources/SingleThreadCore/EntitlementStore.swift:9` with `@MainActor` at `:8`).
- **SingleThreadWidget** — entire block `:997-1058` has no such key; the widget entry builder annotates explicitly: `SingleThreadWidget/NextThingWidget.swift:61` `@MainActor private static func makeEntry()`.
- **All four test targets** — SingleThreadTests (`:835-892`), SingleThreadUITests (`:893-940`), SingleThreadWatchUITests (`:1059-1102`), SingleThreadWatchTests (`:1103-1150`) have no such key. Watch tests annotate explicitly: `SingleThreadWatchTests/ShowCompletionGlowStateTests.swift:23` `@MainActor private let sharedWatchEventStore = EKEventStore()`, `:28` `@MainActor private func watchReminder()`, `:38` `@MainActor @Suite(.serialized)`.
- Project-level configs `:617-734`.

Documented convention: `AGENTS.md:37-41` — "`SWIFT_DEFAULT_ACTOR_ISOLATION = MainActor` is set on the iOS app and watch app targets (not project-wide). Async functions in those targets default to `@MainActor`; do not wrap in `Task { @MainActor in }` there — it's redundant. The `SingleThreadCore` package, widget, and test targets do **not** enable it — annotate `@MainActor` explicitly where needed in those."

Consequence in shared code: the nonisolated `SkippedReminderSyncService.init` cannot use an `@MainActor` default argument, so it creates the entitlement lazily — `SkippedReminderSyncService.swift:61` `self.entitlementStore = entitlementStore ?? MainActor.assumeIsolated { EntitlementStore() }` with comment `:58-60` (see §3).

Package linkage: `XCLocalSwiftPackageReference "SingleThreadCore"` at `project.pbxproj:1226-1229` (`relativePath = SingleThreadCore` `:1228`), product dependency `51AA3F110000000000000002 /* SingleThreadCore */` (`XCSwiftPackageProductDependency`, `:1237`).

---

## 3. Sendable requirements

### The six `Show*Preference` structs are NOT explicitly declared Sendable
None carries `: Sendable`; they are **implicitly Sendable** value types (stored properties `UserDefaults` and `String` are both Sendable):

- `SingleThreadCore/Sources/SingleThreadCore/ShowDatePreference.swift:8` `public struct ShowDatePreference {`; private lets `:29-30` (`private let defaults: UserDefaults`, `private let key: String`).
- Identical shape: `ShowAlarmsPreference.swift:8` (lets `:27-28`), `ShowRecurrencePreference.swift:8` (lets `:27-28`), `ShowListPreference.swift:8` (lets `:27-28`), `ShowCompletionGlowPreference.swift:8` (lets `:27-28`), `ShowUndatedRemindersPreference.swift:10` (lets `:29-30`).
- Read APIs: ShowDatePreference.swift:19-21 `defaults.object(forKey: key) as? Bool ?? true` (+`set(_:)` `:23-25`); ShowAlarmsPreference.swift:19-21; ShowRecurrencePreference.swift:19-21; ShowListPreference.swift:19-21 (`?? false`); ShowCompletionGlowPreference.swift:19-21 (`?? true`); ShowUndatedRemindersPreference.swift:17-19 `load()` (`?? false`) and `save(_:)` `:21-23`.
- Transport: raw `Bool` values inside `[String: Any]` WatchConnectivity contexts built by `SkippedReminderSyncService.swift:176-217` `public func pushAll()` (e.g. `:182` `PayloadKey.showUndatedReminders: showUndatedStore.load()`, `:186-200` the `if sendsShowDate` → `context[PayloadKey.showDate] = showDateStore.isEnabled` pattern, `:213` `PayloadKey.sortOption: sortStore.load().rawValue`). Receipt runs on the WCSession delegate thread: `:320-379` `private func apply(context: [String: Any])` decodes-and-persists each key and fires its handler (e.g. `:342-345` `if let showDate = context[PayloadKey.showDate] as? Bool { showDateStore.set(showDate); let handler = onShowDateReceived; handler?(showDate) }`).
- Receive hooks are `nonisolated(unsafe)` stubs written once before `activate()`: `SkippedReminderSyncService.swift:85` `public nonisolated(unsafe) var onCompleteReminderReceived: ((String) -> Void)?`; `:96` `onShowUndatedRemindersReceived`, `:102` `onShowDateReceived`, `:108` `onShowRecurrenceReceived`, `:114` `onShowAlarmsReceived`, `:120` `onShowListReceived`, `:126` `onShowCompletionGlowReceived`, `:132` `onEntitlementReceived`, `:138` `onCompletionCountReceived`, `:157` `onSortOptionReceived: ((SortOption) -> Void)?`. Rationale documented at `:79-84` ("`nonisolated(unsafe)` because the closure captures the `@MainActor` `ReminderStore` and is therefore not `Sendable`"; write-once-before-`activate()` gives a happens-before edge).

### Explicit Sendable declarations in shared, preference/display-related code
- `SingleThreadCore/Sources/SingleThreadCore/SortOption.swift:6` — `public enum SortOption: String, CaseIterable, Sendable {` — the display-preference enum; its `rawValue` crosses into the payload (`SkippedReminderSyncService.swift:213`) and the receive hook fires on the delegate thread (`:157`). It is the reason preference-like values can be sent: the enum itself is the on-wire value type.
- `SingleThreadCore/Sources/SingleThreadCore/ReminderDisplay.swift:6` — `public struct ReminderDisplay: Equatable, Sendable {` — "Display-ready fields for a single reminder" (`:4-5`); the widget snapshot carried inside `NextThingEntry` (`SingleThreadWidget/NextThingWidget.swift:10-11` `case reminder(ReminderDisplay)`) through `@Sendable` completions `:40` (`getSnapshot(in:completion: @escaping @Sendable (NextThingEntry) -> Void)`) and `:51` (`getTimeline(... @Sendable (Timeline<NextThingEntry>) -> Void)`). `NextThingEntry` is implicitly Sendable.
- `SingleThreadCore/Sources/SingleThreadCore/ReminderDateFilter.swift:23` — `extension EKReminder: @retroactive @unchecked Sendable {}` — "load-bearing for two crossings: 1. `ReminderStore.fetchReminders` resumes a `CheckedContinuation<[EKReminder], Never>` from EventKit's completion queue — `CheckedContinuation` requires its value to be `Sendable`. 2. File-scoped `let` mock globals in targets that do not opt into MainActor-default isolation (the watch/widget targets)." (`:6-22`; SAFETY INVARIANT `:15-17`).
- `SingleThreadCore/Sources/SingleThreadCore/ReminderStore.swift:12` — `public typealias ReminderStoreSettle = @Sendable () async -> Void` — the post-save settle hook shared by all targets.
- `SingleThreadCore/Sources/SingleThreadCore/ResumptionGate.swift:19` — `public final class ResumptionGate: @unchecked Sendable {`; `:46` `public nonisolated func resumeOnMainActor(_ gate: ResumptionGate, _ resume: @escaping @Sendable () -> Void)` — shared off-main-to-main single-resume bridge.
- Other explicit Core Sendable (non-preference): `AppInfo.swift:6` `public struct AppInfo: Sendable`; `TranscriptionAccumulator.swift:18` `public struct TranscriptionAccumulator: Sendable`; `ReminderSkip.swift:32` `public enum Level: CaseIterable, Equatable, Sendable`.
- `pushAll()` avoids sending `self`: `SkippedReminderSyncService.swift:205-210` comment "`EntitlementStore` is main-actor-isolated (implicitly Sendable), so a local snapshot avoids sending `self`." with `:208` `context[PayloadKey.entitled] = MainActor.assumeIsolated { ... }`.

---

## 4. WatchOS `@Observable Show*State` holders — how they bridge preference values

Five structurally identical holders in `SingleThreadWatch/`:

- `SingleThreadWatch/ShowDateState.swift:8-29` `@Observable final class ShowDateState {`:
  - `:12-14` init: `init() { isEnabled = preference.isEnabled }` — one-shot read at construction.
  - `:18` `private(set) var isEnabled: Bool` — observable published state.
  - `:20-24` `func apply(_ value: Bool) { preference.set(value); isEnabled = value }` — persists through the store, then republishes.
  - `:28` `private let preference = ShowDatePreference(defaults: .standard)` — the underlying store, hardcoded to `UserDefaults.standard` (on watchOS `AppGroup.defaults` falls back to `.standard` — `SingleThreadCore/Sources/SingleThreadCore/AppGroup.swift:17-19` `UserDefaults(suiteName: suiteName) ?? .standard`).
- Same pattern: `ShowListState.swift:19-33` (preference at `:28` `ShowListPreference(defaults: .standard)`), `ShowAlarmsState.swift:19-33` (`:28`), `ShowRecurrenceState.swift:19-33` (`:28`), `ShowCompletionGlowState.swift` (preference `ShowCompletionGlowPreference(defaults: .standard)` at `:27`).
- Motivation doc on each: e.g. `ShowDateState.swift:4-7` — "Replaces the former `@AppStorage("showDate")` read-back, whose observation of out-of-band UserDefaults writes is OS-version-dependent; updates now arrive through the sync pipeline's explicit `onShowDateReceived` callback."

Composition and wiring (`SingleThreadWatch/WatchAppViewModel.swift`, `@MainActor` final class at `:9-12`):

- Created in `init` at `:36-40` and stored as lets `:66-70`; injected into `WatchReminderViewModel` via `lazy var reminderViewModel` (`:78-91`, params `:84-90`).
- Launch-time undated restore bypasses the holder: `:34` `store.showsUndatedReminders = ShowUndatedRemindersPreference(defaults: .standard).load()`; `ReminderStore.showsUndatedReminders` is a `didSet` hook property — `ReminderStore.swift:134-138` `didSet { guard showsUndatedReminders != oldValue else { return }; onShowUndatedRemindersChanged?(showsUndatedReminders) }` (`onShowUndatedRemindersChanged` declared `:112-114`).
- Sync service built receive-only with `.standard` stores: `:166-171` (`ShowUndatedRemindersPreference(defaults: .standard)`, `ShowDatePreference(defaults: .standard)`, `ShowRecurrencePreference(defaults: .standard)`, `ShowAlarmsPreference(defaults: .standard)`, `ShowListPreference(defaults: .standard)`, `ShowCompletionGlowPreference(defaults: .standard)`); `:172-173` `sendsShowDate: false, sendsShowRecurrence: false, sendsShowAlarms: false, sendsShowList: false, sendsShowCompletionGlow: false, sendsEntitled: false`.
- `wireStateReceiveHooks(_:)` at `:222-244` maps each delegate-thread hook onto the main actor and into `apply(_:)`, weak-capturing each holder — e.g. `:223-230`:
  `let showDateState = showDateState` ... `service.onShowDateReceived = { [weak showDateState] value in Task { @MainActor in showDateState?.apply(value) } }`
  (recurrence `:231-233`, alarms `:234-236`, list `:237-239`, glow `:240-242`, entitlement `:243-244`).
- UI-test seams pre-set glow state: `:48-52` `if arguments.contains("--ui-testing-glow-disabled") { showCompletionGlowState.apply(false) } else if arguments.contains("--ui-testing-glow") { showCompletionGlowState.apply(true) }`.
- Consumption: `SingleThreadWatch/WatchReminderView.swift:257` `if viewModel.showDateState.isEnabled, let due = display.dueDate {`; `:262` (list), `:267` (recurrence), `:272` (alarms); glow gate in `SingleThreadWatch/WatchReminderViewModel.swift:87` `if await store.completeCurrentReminder(), showCompletionGlowState.isEnabled {`.
- Bridging proven by test: `SingleThreadWatchTests/ShowCompletionGlowStateTests.swift:41-54` — `#expect(!state.isEnabled, "initial value comes from the persisted preference")`, `#expect(!ShowCompletionGlowPreference(defaults: .standard).isEnabled, "apply persists into the preference store")`, `#expect(state.isEnabled, "apply republishes true through the state")`.

The iOS write side of the same keys: `SingleThread/ContentView.swift:115-133` — `@AppStorage("showUndatedReminders", store: AppGroup.defaults)`, `@AppStorage("showDate", store: AppGroup.defaults)`, `"showList"`, `"showRecurrence"`, `"showAlarms"`, `"showCompletionGlow"`, `@AppStorage(SortOption.defaultsKey, store: AppGroup.defaults)`. Change → push chain: `SingleThread/AppViewModel.swift:413-429` `setupSyncObservation()` observes `UserDefaults.didChangeNotification` on `AppGroup.defaults`; `:431-449` `handlePreferencesChanged()` diffs `ShowDatePreference().isEnabled` etc. (`:433-437`) against `lastShowDate`/`lastShowRecurrence`/`lastShowAlarms`/`lastShowList`/`lastShowCompletionGlow` (`:453-457`) and calls `syncService?.pushAll()` on any change. Full round trip: Settings bindings → `@AppStorage` (`SingleThread/ContentView+Settings.swift:37-43`) → AppGroup suite → NotificationCenter observation → `pushAll()` → WCSession context → watch `apply(context:)` → `onShow*Received` → `Task { @MainActor in state?.apply(value) }` → `preference.set(value)` + `isEnabled = value`.

---

## 5. `#if os(...)` conditional compilation

- **`SingleThreadWatch/`** (app + `SingleThreadWatchTests/`): **zero** `#if os(...)`/`#elseif`/`#endif` occurrences (grep: no matches).
- **`SingleThreadWidget/`**: **zero** occurrences (grep: no matches).
- **`SingleThreadCore/`**:
  - `SkippedReminderSyncService.swift:4` `#if os(iOS) || os(watchOS)` wraps the entire file (imports `WatchConnectivity` `:3-5`); `:267` `#if os(iOS)` gates `sessionDidBecomeInactive` / `sessionDidDeactivate` (`:268-273`). This preference-sync pipeline is therefore iOS+watchOS only; macOS/widget builds exclude the whole file body.
  - `ReminderStore.swift` (shared store for all surfaces): `:203` `#if os(watchOS)` (complete branch), `:279` (delete branch), `:311` (`addReminder` returns `false`), `:337` (`rescheduleReminder` returns `false`), `:361` `#if os(iOS) || os(watchOS)` (6th-skip nudge); `#if !os(watchOS)` at `:124` (`undoStore` property), `:246` (`undoLastCompletion`), `:418` (`refreshSourcesIfNecessary`). These are EventKit-mutation branches, not preference gates.
  - `CodeSpanFormatter.swift:137-139` `#if canImport(SwiftUI)` then `#if os(watchOS)` (secondary background color — display, not preference).
- **`SingleThread/`** (iOS/macOS app) — many `#if os(iOS)`/`#if os(macOS)` blocks; the **preference-relevant** ones:
  - `AppViewModel.swift:4-8` `#if os(iOS)` imports EventKit/UserNotifications/WatchConnectivity; `:9-11` `#if os(iOS) || os(macOS)` imports WidgetKit.
  - `AppViewModel.swift:28-76` `#if os(iOS)` — the whole WatchConnectivity sync-service construction (iOS-side stores `ShowDatePreference()`/`ShowRecurrencePreference()`/`ShowAlarmsPreference()`/`ShowCompletionGlowPreference()` at `:33-36`, `sendsShowDate: true, sendsEntitled: true` at `:37-38`).
  - `AppViewModel.swift:77-81` `#if os(iOS) || os(macOS)` — `store.onRemindersChanged = { WidgetCenter.shared.reloadAllTimelines() }`.
  - `AppViewModel.swift:87-95` `#if os(iOS)` — `setupSyncObservation(); setupEntitlementObservation()`.
  - `AppViewModel.swift:377-460` one `#if os(iOS)` block holding `setupEntitlementObservation` (`:377-395`), `setupSyncObservation` (`:413-429`) and `handlePreferencesChanged` + `lastShow*` + `syncDefaultsObserver` (`:431-460`) — the entire AppGroup→watch push observation exists only on iOS.
  - Other blocks: `:195-202`, `:277-313` (`#if os(iOS)`); `ContentView.swift:9-11`, `:22-24`, `:37-39`, `:63-65`, `:78-114`, `:147-153`, `:200-229`, `:253-325`, `:328-371` (`#if os(macOS)`), `:447-467`, `:503-542`, `:593-700` (layout/type branches; the `@AppStorage` show* properties at `:115-133` are not os-gated); `AppDelegate.swift:1-60` `#if os(iOS)` / `:62-92` `#if os(macOS)`; `BackgroundImageStore.swift:3-7`, `:242-246`, `:289-293`; `InterfaceSettingsView.swift:13-15`; `Color+CrossPlatform.swift:3-7`, `:16-20`; `SingleThreadApp.swift:2-7`, `:32-39`.

The six `Show*Preference` files themselves contain **no** `#if` blocks — their UserDefaults access is fully cross-platform.

---

## 6. Lint/format rules applied to these files

### `.swiftlint.yml` (root, 92 lines)
- **`included`** `:2-9`: `SingleThread`, `SingleThreadCore`, `SingleThreadWatch`, `SingleThreadWidget`, `SingleThreadTests`, `SingleThreadUITests`, `SingleThreadWatchUITests`. **`SingleThreadWatchTests` is NOT in `included`** — the watch unit-test directory is not linted by the root config.
- **`disabled_rules`** `:12-18`: `single_test_class`, `balanced_xctest_lifecycle`, `empty_xctest_method`, `final_test_case`, `multiple_closures_with_trailing_closure`, `type_name` (comment `:11`: "XCTest-specific rules disabled — project uses Swift Testing").
- **Thresholds**:
  - `line_length` `:21-23`: `warning: 120`, `error: 150`.
  - `cyclomatic_complexity` `:25-27`: `warning: 12`, `error: 15`.
  - `type_body_length` `:29-31`: `warning: 500`, `error: 600`.
  - `file_length` `:33-35`: `warning: 650`, `error: 800` (invoked by `ReminderStore.swift:5` `// swiftlint:disable file_length`).
- **Force-casting** `:37-41` (comment `:37` "Allow force-casting in test code"): `force_cast: severity: warning` (`:38-39`), `force_try: severity: warning` (`:40-41`).
- **`opt_in_rules`** `:44-79` — exactly **35 rules**: `accessibility_label_for_image` (:45), `accessibility_trait_for_button` (:46), `array_init` (:47), `attributes` (:48), `closure_spacing` (:49), `contains_over_filter_is_empty` (:50), `contains_over_first_not_nil` (:51), `discouraged_optional_boolean` (:52), `empty_collection_literal` (:53), `empty_count` (:54), `empty_string` (:55), `explicit_init` (:56), `fallthrough` (:57), `fatal_error_message` (:58), `first_where` (:59), **`force_unwrapping` (:60)** — the force-unwrap ban, `function_default_parameter_at_end` (:61), `implicit_return` (:62), `legacy_multiple` (:63), `legacy_objc_type` (:64), `overridden_super_call` (:65), `prefer_self_in_static_references` (:66), `prohibited_super_call` (:67), `redundant_type_annotation` (:68), `self_binding` (:69), `shorthand_optional_binding` (:70), `sorted_imports` (:71), `static_over_final_class` (:72), `toggle_bool` (:73), `trailing_closure` (:74), `unhandled_throwing_task` (:75), `unneeded_synthesized_initializer` (:76), `unowned_variable_capture` (:77), `vertical_parameter_alignment_on_call` (:78), `yoda_condition` (:79).
- **`analyzer_rules`** `:81-82`: `unused_import`.
- **`identifier_name`** `:85-92` — contains **only** `excluded:` (`id`, `e`, `d`, `rt`, `to`, `gvm`); there is **no `min_length` (and no warning/error) override** anywhere in the config, so the SwiftLint default applies (min_length warning 3 / error 2). The task-prompted "identifier_name min length" value is not configured in this repo.

### `.swiftformat` (21 lines)
`:2` `--swiftversion 6.0`; `:5` `--indent 4`; `:6` `--wraparguments before-first`; `:7` `--wrapcollections before-first`; `:8` `--closingparen same-line`; `:11` `--enable blankLinesAroundMark`; `:12` `--enable organizeDeclarations`; `:13` `--enable preferSwiftTesting`; `:14` `--disable andOperator`; `:15` `--disable isEmpty`; `:16` `--disable trailingClosures`; `:17` `--disable trailingCommas`; `:18` `--disable wrapMultilineStatementBraces`; `:21` `--exclude SingleThreadUITests`.

### Test-target relaxations (per-directory config)
- Only **one** per-directory config exists: `SingleThreadTests/.swiftlint.yml` (3 lines): `# Relax rules for test code — force-unwrapping test fixtures and known-valid date literals is idiomatic in Swift Testing.` then `disabled_rules:` / `  - force_unwrapping`. It turns **off** the `force_unwrapping` opt-in rule for the `SingleThreadTests` directory only; the other 34 opt-in rules and all thresholds still apply.
- `SingleThreadWatchTests/`, `SingleThreadUITests/`, `SingleThreadWatchUITests/` have **no** per-directory `.swiftlint.yml` (repo-wide find: only root + `SingleThreadTests/`). `force_cast`/`force_try` stay warning-severity everywhere; `force_unwrapping` is active everywhere except `SingleThreadTests`.

### Invocation
- `Makefile:100-101` (`lint`): `swiftformat --lint SingleThread/ SingleThreadCore/ SingleThreadWatch/ SingleThreadWidget/ SingleThreadTests/ SingleThreadUITests/ SingleThreadWatchUITests/ SingleThreadWatchTests/` then `swiftlint lint --strict`.
- `Makefile:103-104` (`format`): `swiftformat <same eight dirs>` then `swiftlint --fix`. (Note swiftformat is passed all eight directories, including both test bundles; `SingleThreadUITests` is then excluded via `.swiftformat:21`.)
- CI mirrors exactly: `.github/workflows/ci.yml:350` `run: swiftformat --lint <same eight dirs>`; `:354` `run: swiftlint lint --strict`.
- Source comments cite the default function-body limit the config does not override: `WatchAppViewModel.swift:218-219` "Lives in its own helper so `setupSyncService` stays within SwiftLint's 50-line function-body limit." and `SkippedReminderSyncService.swift:298-300` "Kept in its own method so `apply(context:)` stays within SwiftLint's 50-line function-body limit." — i.e. the default `function_body_length` (warning 50 / error 100) is in force.

---

## Explicitly not found
- Any explicit `Sendable` conformance on the six `Show*Preference` structs (implicit Sendable only).
- Any `SWIFT_STRICT_CONCURRENCY` setting in `project.pbxproj` or the package manifest.
- Any `identifier_name.min_length`/severity override in `.swiftlint.yml` or `SingleThreadTests/.swiftlint.yml`.
- Any `#if os(...)` in `SingleThreadWatch/` or `SingleThreadWidget/`.
- Any per-directory `.swiftlint.yml` beyond `SingleThreadTests/.swiftlint.yml`.
