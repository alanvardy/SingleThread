# Task: Microphone not visible (VAR-747)

One user reports they cannot see the microphone (dictation) button in the
SingleThread app; the developer cannot reproduce it on their own device. The
goal is to determine under what circumstances the mic button can be hidden or
absent, and (eventually) fix the underlying cause so the mic is visible when
it should be. The current branch contains only a placeholder `DELETEME` file —
no investigation or fix exists yet.